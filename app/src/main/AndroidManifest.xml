<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/iconoapp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iconoapp_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaxStorage"
        tools:targetApi="31">
        <activity
            android:name=".Menu.menu"
            android:exported="false" />
        <activity
            android:name=".Almacen.AlmacenPaquetesActivity"
            android:exported="false" />
        <activity
            android:name=".Login.registro"
            android:exported="false" />
        <activity
            android:name=".Paquetes.PaquetesActivity_Accion"
            android:exported="false" />
        <activity
            android:name=".Login.Login"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>